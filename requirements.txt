# Core
torch>=1.10
numpy>=1.21
scipy>=1.7

# Optional LP solver backends for the fluid model (auto-priority: gurobi > docplex > scipy)
# gurobipy
# docplex

# Live training curves (default-on for experiments/run_ablation.py; the code
# degrades gracefully to CSV-only logging if the visdom server is not running)
visdom

# Publication figures (experiments/plot_figures.py)
matplotlib

# Optional: legacy plotting utilities (utilities/Utility_Class.py)
# pandas
# openpyxl
